# Zeabur 部署配置
# 此配置文件用於 Zeabur 平臺自動部署

{
  "framework": "flask",
  "python_version": "3.11",
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "gunicorn app:app --bind 0.0.0.0:${PORT:-8080}",
  "envs": {
    "PORT": "8080"
  }
}
